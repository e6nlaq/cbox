
name: Windows Test

on: 
  pull_request:
    types: [opened,synchronize]
  push:
    branches: [main]

jobs:
    build:
        runs-on: windows-latest

        steps:
          -  uses: actions/checkout@v4

          - name: test
            shell: powershell
            run: |
              git --version
              cmake --version
